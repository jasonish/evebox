A docker-compose file for running EveBox locally against PostgreSQL
(primarily for development purposes).

Usage
-----

    docker-compose up
	
Now connect EveBox to PostgreSQL at localhost:5432 with:
- username: evebox
- password: evebox
- database: evebox
